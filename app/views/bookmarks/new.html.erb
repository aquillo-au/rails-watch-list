<h1>Create Bookmark for List: <%= @list.name %></h1>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.all, input_html: {data: {controller: "tom-select"}} %>
  <%= f.submit %>
<% end %>
